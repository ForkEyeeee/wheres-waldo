import { BrowserRouter as Router } from "react-router-dom";
import App from "../App";

const RouterConfig = () => {
  return (
    <Router>
      <App />
    </Router>
  );
};

export default RouterConfig;
